spring:
  datasource:
    url: jdbc:mysql://mysql:3306/versiondb?allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: "1234554321"

eureka:
  client:
    service-url:
      defaultZone: http://discovery-server:8761/eureka/
