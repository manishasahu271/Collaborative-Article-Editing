server:
  port: 9002
  
spring:
  application:
    name: ARTICLE-SERVICE
  kafka:
    bootstrap-servers: localhost:9092
    
eureka:
  client:
    service-url:
      defaultZone : http://localhost:8761/eureka/