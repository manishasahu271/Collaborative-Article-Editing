MYSQL_ROOT_PASSWORD=change-me-now

# Optional: only needed later if you enable the `domain` profile
# and want Caddy to serve the gateway over HTTPS.
API_DOMAIN=api.example.com
ACME_EMAIL=you@example.com
