spring:
  datasource:
    url: jdbc:mysql://mysql:3306/userdb?allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: "1234554321"
  kafka:
    bootstrap-servers: kafka:29092

eureka:
  client:
    service-url:
      defaultZone: http://discovery-server:8761/eureka/
